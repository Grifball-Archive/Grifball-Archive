.DS_Store
Thumbs.db
desktop.ini

# Jekyll (local builds)
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
vendor/

# MkDocs
site/
.cache/

# Local planning / design notes (not shared via git — see README if we add a committed template later)
PLANNING.local.md
